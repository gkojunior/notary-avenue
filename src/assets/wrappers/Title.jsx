import styled from 'styled-components'

const Wrapper = styled.section`
	.underline {
		width: 8rem;
		height: 0.25rem;
		background: blue;
		margin-left: auto;
		margin-right: auto;
		margin-top: 1rem;
	}
`
